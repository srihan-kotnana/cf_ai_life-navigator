
---

##  `PROMPTS.md`

```md
# PROMPTS USED

### code generation
> "generate a worker script with routes.ts and a durable object for session state."

### debugging
> "fix the 'worker is not an actor' error in wrangler dev."

### deployment help
> "wrangler deploy --latest keeps giving 'noted.' response — what’s wrong?"
